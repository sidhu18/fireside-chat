// Server config - todo - move to env
export const HOST = 'localhost'
export const PORT = '11434';
export const API_URL = `http://${HOST}:${PORT}/api/generate`

// Model config
export const MODEL_NAME = 'codellama'
